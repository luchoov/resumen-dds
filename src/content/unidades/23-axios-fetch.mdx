---
numero: 23
titulo: 'Axios, Fetch e interceptores'
subtitulo: 'Consumir APIs REST desde React, con patrón de servicio e interceptores.'
bloque: 'React: navegación y datos'
semana: 'Semana 12'
color: data
icon: 'plug'
resumen: 'Axios es una librería basada en promesas para peticiones HTTP que mejora a fetch (JSON automático, errores claros, interceptores); conviene encapsular las llamadas en una capa de servicio reutilizable.'
tags: ['Axios', 'Fetch', 'API REST', 'Servicios', 'Interceptores']
---

## ¿Qué es Axios?

**Axios** es una librería basada en **promesas** para hacer peticiones HTTP, tanto en el **frontend** (React) como en el **backend** (Node). Es muy popular por su simplicidad y por funciones que `fetch` no trae de fábrica.

<div class="conceptos">
<article class="concepto"><div class="concepto__t">JSON automático</div><div class="concepto__d">Transforma la respuesta a JSON sin que llames a <code>.json()</code>.</div></article>
<article class="concepto"><div class="concepto__t">Errores claros</div><div class="concepto__d">Un status distinto de 2xx lanza error automáticamente.</div></article>
<article class="concepto"><div class="concepto__t">Interceptores</div><div class="concepto__d">Modificar peticiones/respuestas de forma centralizada (auth, logs).</div></article>
<article class="concepto"><div class="concepto__t">Config global</div><div class="concepto__d"><code>baseURL</code>, headers y timeouts compartidos por todas las llamadas.</div></article>
</div>

## Axios vs Fetch

| Característica | `fetch` (nativo) | `axios` (librería) |
|---|---|---|
| JSON automático | Manual (`.json()`) | Sí |
| Errores HTTP | No lanza en 4xx/5xx | Lanza solo |
| Cancelación | Experimental | Integrada |
| Interceptores | No | Sí |
| Sintaxis | Verbosa | Concisa |

<Callout type="info" title="¿Hace falta Axios?">
No: React funciona perfecto con `fetch` nativo. Pero Axios **ahorra código repetitivo** y resuelve cosas (errores, interceptores, baseURL) que con `fetch` tenés que armar a mano.
</Callout>

## Los métodos principales

```js
axios.get(url[, config]);        // leer
axios.post(url, data[, config]); // crear
axios.put(url, data[, config]);  // reemplazar
axios.delete(url[, config]);     // borrar
```

## Mismo ejemplo: Fetch vs Axios

Usando la API pública **JSONPlaceholder** (un backend falso para practicar). Primero con `fetch`:

```jsx
import { useEffect, useState } from 'react';

export default function ListaPosts() {
  const [posts, setPosts] = useState([]);

  useEffect(() => {
    fetch('https://jsonplaceholder.typicode.com/posts?userId=1')
      .then((res) => {
        if (!res.ok) throw new Error('Error en la carga');
        return res.json();            // hay que parsear a mano
      })
      .then(setPosts)
      .catch((err) => console.error(err));
  }, []);

  return <ul>{posts.map((p) => <li key={p.id}>{p.title}</li>)}</ul>;
}
```

Ahora con **Axios** (más limpio, con `baseURL`):

```jsx
import axios from 'axios';

const api = axios.create({ baseURL: 'https://jsonplaceholder.typicode.com' });

useEffect(() => {
  api.get('/posts', { params: { userId: 1 } })
    .then((response) => setPosts(response.data)) // ya viene parseado
    .catch((error) => console.error(error));
}, []);
```

<Callout type="warn" title="response.data, no response">
Axios envuelve la respuesta: los datos están en **`response.data`**, no en `response` directamente. Con `fetch` accedés tras `await res.json()`.
</Callout>

## Patrón de servicio (separar responsabilidades)

En vez de poner las llamadas HTTP dentro de los componentes, se **encapsulan en una capa de servicio**. El componente solo se preocupa por **mostrar**.

<Figura caption="El componente usa el servicio; el servicio habla con la API. Cada capa, una responsabilidad.">
<svg viewBox="0 0 620 110" width="620" height="110" xmlns="http://www.w3.org/2000/svg" font-family="Inter Variable, sans-serif">
  <rect x="20" y="35" width="150" height="48" rx="11" fill="var(--surface-2)" stroke="hsl(var(--c-react))"/>
  <text x="95" y="58" font-size="12" fill="var(--text)" text-anchor="middle" font-weight="700">Componente</text>
  <text x="95" y="74" font-size="10" fill="var(--text-dim)" text-anchor="middle">muestra</text>

  <rect x="235" y="35" width="150" height="48" rx="11" fill="var(--surface-2)" stroke="hsl(var(--c-data))"/>
  <text x="310" y="58" font-size="12" fill="var(--text)" text-anchor="middle" font-weight="700">postService</text>
  <text x="310" y="74" font-size="10" fill="var(--text-dim)" text-anchor="middle">lógica de acceso</text>

  <rect x="450" y="35" width="150" height="48" rx="11" fill="var(--surface-2)" stroke="hsl(var(--c-express))"/>
  <text x="525" y="58" font-size="12" fill="var(--text)" text-anchor="middle" font-weight="700">api (axios)</text>
  <text x="525" y="74" font-size="10" fill="var(--text-dim)" text-anchor="middle">baseURL + HTTP</text>

  <line x1="170" y1="59" x2="233" y2="59" stroke="var(--text-dim)" stroke-width="1.8" marker-end="url(#s23)"/>
  <line x1="385" y1="59" x2="448" y2="59" stroke="var(--text-dim)" stroke-width="1.8" marker-end="url(#s23)"/>
  <defs><marker id="s23" markerWidth="9" markerHeight="9" refX="7" refY="3" orient="auto"><path d="M0,0 L7,3 L0,6 Z" fill="var(--text-dim)"/></marker></defs>
</svg>
</Figura>

```js
// services/api.js — instancia reutilizable
import axios from 'axios';
export const api = axios.create({ baseURL: 'https://jsonplaceholder.typicode.com' });
```

```js
// services/postService.js — centraliza las operaciones del recurso
import { api } from './api';
const resource = '/posts';

export const postService = {
  getAllByUser: (userId) => api.get(resource, { params: { userId } }),
  create: (post) => api.post(resource, post),
  update: (id, post) => api.put(`${resource}/${id}`, post),
  remove: (id) => api.delete(`${resource}/${id}`),
};
```

```jsx
// App.jsx — el componente solo consume el servicio
import { postService } from './services/postService';

useEffect(() => {
  postService.getAllByUser(1)
    .then((res) => setPosts(res.data))
    .catch((err) => console.error(err));
}, []);
```

<Callout type="tip" title="Ventajas del patrón de servicio">
**Reutilización** (la misma lógica desde varios componentes), **separación de responsabilidades** (el componente solo muestra) y **mantenimiento/testing** más fáciles. Escala agregando `usuariosService.js`, etc.
</Callout>

<Callout type="warn" title="Mientras llega la respuesta, los datos son null">
La petición es **asíncrona**: en el **primer render** los datos todavía no llegaron (por eso el estado arranca en `useState(null)`). Si hacés `data.map(...)` directamente, explota con *Cannot read properties of null*. Por eso se usa **renderizado condicional**: `{data ? <Tabla /> : <p>Cargando…</p>}`. El `useEffect` con `[]` dispara la carga **una sola vez** al montar; si lo omitieras, como adentro se hace `setData`, cada render volvería a pedir → re-render → pedir… un **loop infinito** de peticiones.
</Callout>

## Interceptores

Los **interceptores** son funciones que se ejecutan **antes** de enviar una petición o **después** de recibir una respuesta. Permiten aplicar lógica común a **todas** las llamadas desde un solo lugar.

<Figura caption="Los interceptores se enganchan en la salida (request) y la entrada (response).">
<svg viewBox="0 0 620 130" width="620" height="130" xmlns="http://www.w3.org/2000/svg" font-family="Inter Variable, sans-serif">
  <rect x="20" y="45" width="120" height="44" rx="11" fill="var(--surface-2)" stroke="var(--border)"/>
  <text x="80" y="71" font-size="12" fill="var(--text)" text-anchor="middle" font-weight="700">App</text>

  <rect x="245" y="45" width="130" height="44" rx="11" fill="color-mix(in srgb, hsl(var(--c-data)) 14%, var(--surface))" stroke="hsl(var(--c-data))"/>
  <text x="310" y="65" font-size="11" fill="var(--text)" text-anchor="middle" font-weight="700">Interceptores</text>
  <text x="310" y="80" font-size="9" fill="var(--text-dim)" text-anchor="middle">request / response</text>

  <rect x="480" y="45" width="120" height="44" rx="11" fill="var(--surface-2)" stroke="hsl(var(--c-express))"/>
  <text x="540" y="71" font-size="12" fill="var(--text)" text-anchor="middle" font-weight="700">Servidor</text>

  <line x1="140" y1="58" x2="243" y2="58" stroke="hsl(var(--c-data))" stroke-width="1.8" marker-end="url(#i23)"/>
  <text x="192" y="50" font-size="9" fill="var(--text-dim)" text-anchor="middle">+ token</text>
  <line x1="375" y1="58" x2="478" y2="58" stroke="hsl(var(--c-data))" stroke-width="1.8" marker-end="url(#i23)"/>
  <line x1="478" y1="78" x2="143" y2="78" stroke="hsl(var(--c-express))" stroke-width="1.8" marker-end="url(#i23b)"/>
  <text x="310" y="100" font-size="9" fill="var(--text-dim)" text-anchor="middle">manejo de errores global</text>
  <defs>
    <marker id="i23" markerWidth="9" markerHeight="9" refX="7" refY="3" orient="auto"><path d="M0,0 L7,3 L0,6 Z" fill="hsl(var(--c-data))"/></marker>
    <marker id="i23b" markerWidth="9" markerHeight="9" refX="7" refY="3" orient="auto"><path d="M0,0 L7,3 L0,6 Z" fill="hsl(var(--c-express))"/></marker>
  </defs>
</svg>
</Figura>

**Agregar el token de autenticación a todas las peticiones:**

```js
api.interceptors.request.use((config) => {
  config.headers.Authorization = `Bearer ${localStorage.getItem('authToken')}`;
  return config;
});
```

**Manejar errores de forma global:**

```js
api.interceptors.response.use(
  (response) => response,                 // si todo OK, pasa
  (error) => {
    console.error('Se produjo un error:', error.message);
    return Promise.reject(error);         // rechaza para manejarlo arriba
  }
);
```

<Callout type="analogia" title="Interceptores = control de aduana">
Toda petición que **sale** pasa por la aduana de salida (le ponen el sello del token); toda respuesta que **entra** pasa por la de llegada (revisan si hubo error). Lo escribís **una vez** y aplica a todo el tráfico.
</Callout>

<Callout type="danger" title="localStorage NO es 'totalmente seguro'">
El JWT viaja en el header **`Authorization: Bearer <token>`**. Guardarlo en `localStorage` es cómodo, pero **cualquier JavaScript que corra en tu página puede leerlo** con `localStorage.getItem(...)`. Si tu sitio tiene una vulnerabilidad de **XSS** (Cross-Site Scripting — ver Apunte 17), un script inyectado roba el token y **suplanta al usuario**. El navegador **no** lo protege. Mitigaciones: prevenir el XSS (escapar/sanitizar entradas, CSP) y, para apps sensibles, guardar el token en una **cookie `HttpOnly`** (inaccesible desde JS).
</Callout>

## Autoevaluación

<p class="muted">Preguntas del estilo del Parcial 2. Respondé mentalmente (o en papel) y recién después abrí la respuesta.</p>

<details class="qa">
<summary>¿Qué es Axios? Mapeá <code>get/post/put/delete</code> con verbo HTTP y operación CRUD. Dos ventajas sobre <code>fetch</code>.</summary>
<div class="qa__a">

Axios es una librería basada en **promesas** para peticiones HTTP (front y back).

- `axios.get` → **GET** → **leer** (`/api/articulos`).
- `axios.post` → **POST** → **crear** (`/api/articulos`).
- `axios.put` → **PUT** → **reemplazar/actualizar** (`/api/articulos/5`).
- `axios.delete` → **DELETE** → **borrar** (`/api/articulos/5`).

Ventajas vs `fetch`: convierte la respuesta a **JSON automáticamente** y **lanza error** solo ante un status 4xx/5xx (con `fetch` ambas son manuales). También: interceptores y `baseURL`.

</div>
</details>

<details class="qa">
<summary>¿Por qué poner la llamada a Axios en un servicio separado? ¿Por qué el <code>useEffect</code> lleva <code>[]</code>? ¿Qué pasa si se omite?</summary>
<div class="qa__a">

El **patrón de servicio** separa responsabilidades: el componente solo **muestra**, el servicio concentra el acceso a datos → reutilizable, testeable y mantenible (mismos principios de arquitectura del Apunte 12).

El `[]` hace que el efecto corra **una sola vez** (al montar). Si se omite, como adentro se hace `setArticulos`, cada render dispararía la carga → `setState` → re-render → carga… **loop infinito** de peticiones.

</div>
</details>

<details class="qa">
<summary>En un componente que arranca con <code>useState(null)</code> y carga datos: ¿qué ve el usuario en orden? ¿Qué error da si se hace <code>data.map(...)</code> sin renderizado condicional?</summary>
<div class="qa__a">

Orden: primero `data` es `null` → se muestra **"Cargando datos…"**; cuando la promesa resuelve, el `setArticulos(data)` actualiza el estado → **re-render** → se muestra la tabla. El evento que provoca el cambio es la **resolución de la promesa** de Axios.

Sin renderizado condicional, en el primer render `data` es `null` y `null.map(...)` lanza **`Cannot read properties of null (reading 'map')`**. Por eso `{data ? <Tabla /> : <p>Cargando…</p>}`.

</div>
</details>

<details class="qa">
<summary>¿Qué es un interceptor? Diferencia request vs response. Un compañero dice que guardar el JWT en <code>localStorage</code> "es totalmente seguro". ¿Qué le observás?</summary>
<div class="qa__a">

Un **interceptor** es una función que corre **antes** de enviar una petición (request) o **después** de recibir una respuesta (response). Es un punto **centralizado** para lógica transversal: el de **request** sirve para inyectar el token a todas las llamadas; el de **response**, para manejo de errores global.

```js
axios.interceptors.request.use((config) => {
  config.headers.Authorization = `Bearer ${localStorage.getItem('authToken')}`;
  return config;
});
```

Lo que se le observa: `localStorage` **no es seguro**. Cualquier JS de la página puede leerlo, así que una vulnerabilidad de **XSS** permite **robar el token** y suplantar al usuario. El navegador no lo protege. Para datos sensibles conviene una cookie **`HttpOnly`** + prevenir XSS.

</div>
</details>

<Callout type="parcial">
- **Axios** = librería de peticiones HTTP basada en promesas (front y back).
- Ventajas vs `fetch`: **JSON automático**, **lanza error** en 4xx/5xx, **interceptores**, `baseURL`.
- Los datos llegan en **`response.data`** (no en `response`).
- Métodos: `get`, `post`, `put`, `delete`.
- **Patrón de servicio**: encapsular las llamadas en `services/` → el componente solo muestra.
- **Interceptores**: lógica común a todas las peticiones (token) o respuestas (errores) en un solo lugar.
</Callout>
